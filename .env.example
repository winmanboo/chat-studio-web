# 后端 API 服务地址
# 这个地址将被 Next.js API 路由用于代理请求到后端服务
BACKEND_API_URL=http://127.0.0.1:8080

# Service Worker 配置
# 在开发环境下禁用 Service Worker（true=禁用，false=启用）
DISABLE_SERVICE_WORKER_IN_DEV=true

# 版本检查间隔（毫秒）
# 开发环境建议: 10000 (10秒)
# 生产环境建议: 60000 (60秒)
SW_VERSION_CHECK_INTERVAL=60000

# 注意：
# 1. 复制此文件为 .env.local 并根据你的环境修改相应的值
# 2. 不要将包含敏感信息的 .env.local 文件提交到版本控制系统
# 3. 开发环境可以设置 DISABLE_SERVICE_WORKER_IN_DEV=true 来禁用 Service Worker
# 4. 生产环境应该设置 DISABLE_SERVICE_WORKER_IN_DEV=false 或删除该变量